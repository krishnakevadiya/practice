function cascadeResults(...propFuncs) {
  //  complete the function here
  x=propFuncs;
  return function(x){
    if()
  }
}
let x=promptFuncs
console.log(cascadeResults(x){
  (x) = x * 3,
  (x) =x + 1,
  (x) = x / 2
  return x

}
}

export default cascadeResults;
